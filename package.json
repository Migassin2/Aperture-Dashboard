{
  "name": "aperture-光瞳-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "A JAMstack bilingual e-commerce dashboard with prebuilt data.",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist && mkdir dist"
  },
  "devDependencies": {
    "fs-extra": "^11.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
